# Architecture Decision Records

Architecture Decision Records capture decisions that change a module boundary,
public contract, data ownership, deployment topology, security posture, or an
established repository convention.

Copy `template.md` to the next zero-padded number, for example
`0001-extract-orders-service.md`. Keep accepted records immutable; supersede an
earlier decision with a new record instead of rewriting its history.

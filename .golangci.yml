version: "2"

run:
  tests: true

linters:
  default: standard
  enable:
    - bodyclose
    - contextcheck
    - depguard
    - errorlint
    - gocritic
    - gosec
    - misspell
    - noctx
    - revive
    - rowserrcheck
    - sloglint
    - unconvert
  settings:
    depguard:
      rules:
        business-layer:
          list-mode: lax
          files:
            - "**/internal/widget/*.go"
            - "!**/internal/widget/handler.go"
            - "!**/internal/widget/repository.go"
            - "!$test"
          deny:
            - pkg: github.com/go-chi/chi/v5
              desc: business code must not depend on the HTTP router
            - pkg: github.com/jackc/pgx/v5
              desc: business code must not depend on the PostgreSQL driver
            - pkg: github.com/lukuku-dev/go-chi-bp/internal/widget/dbgen
              desc: generated database code must stay behind repositories
            - pkg: github.com/lukuku-dev/go-chi-bp/internal/platform/config
              desc: business code must receive validated values through constructors
  exclusions:
    generated: strict

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: strict
  settings:
    goimports:
      local-prefixes:
        - github.com/lukuku-dev/go-chi-bp

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
